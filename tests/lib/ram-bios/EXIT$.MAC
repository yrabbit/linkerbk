;; vim: set expandtab ai :
;; Инициализирует RAM-BIOS (в частности очищает стек возвратов из подпрограмм,
;; вызванных по запросам CAL$G и CAL$N) и выходит в режим ``DOS'',
;; обычно на 100000.
;; Таким образом, обеспечивается корректный выход в ДОС из любого режима. Также
;; очищает стек сохранений (используемый запросами SAVE$ и NEW$SP).
;; Кроме того удаляются модули, у которых установлен признак удаления по EXIT$,
;; а также все модули. у которых отсутствует родительский модуль и при 
;; этом их удалять не запрещено.
                .MACRO  EXIT$
                JSR     R5,@#167527
                .ENDM

