;; vim: set expandtab ai textwidth=80:
;; Ќазначить/получить рабочую страницу
;; если 15-ый бит = 0 --- установить страницу
;;                = 1 --- получить страницы (R0 ст.байт дл€ вызовов, 
;;                                           мл.байт дл€ чтени€/записи
;; если 7-ой бит = 1 --- установить дл€ вызова подпрограмм
;;                 0 --- установить дл€ чтени€/записи
                .MACRO  .BWORK,ARG
                mov     ARG,R0
                jsr     PC,@140036
                .ENDM

